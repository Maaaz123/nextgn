# Standalone dbt + dbt-dremio (no Airflow; avoids opentelemetry/protobuf conflict)
FROM python:3.11-slim

RUN pip install --no-cache-dir \
    dbt-core==1.10.4 \
    dbt-dremio==1.10.0 \
    pandas \
    pyarrow \
    boto3

WORKDIR /dbt
