# Copy to .env and adjust. Used by docker compose.
# Stack: MinIO + Dremio + Airflow (SQLite) + Metabase (H2). No Postgres by default.

# Airflow UI (admin / admin by default)
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=admin

# MinIO (S3-compatible storage for Iceberg)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# DBT: host path for dbt project (required for DockerOperator; set to absolute path of ./dbt)
# Example: DBT_PROJECT_HOST_PATH=/Users/you/project/data tools/dbt
# DBT_PROJECT_HOST_PATH=

# Dremio (for dbt --target dremio_dev; in Docker use DREMIO_HOST=dremio)
# DREMIO_USER=dremio
# DREMIO_PASSWORD=dremio

# MySQL (for ingestion; add mysql_source connection in Airflow)
# MYSQL_ROOT_PASSWORD=mysql
# MYSQL_DATABASE=mydb
# MYSQL_USER=mysql
# MYSQL_PASSWORD=mysql

# Optional: when using docker-compose.postgres.yml (Postgres as warehouse)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# WAREHOUSE_HOST=postgres
# WAREHOUSE_DB=warehouse
